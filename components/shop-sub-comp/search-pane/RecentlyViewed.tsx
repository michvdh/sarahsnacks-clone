const RecentlyViewed: React.FC = () => {
  // create a "Recently Viewed" state that contains an array of strings
    // specifically productIDs
    // min of 0 items, max of 5 items
  // if an item is clicked and loaded the ProductCompleteDetails component, a function in the ProductCompleteDetails will update the "Recently Viewed"
    // check state array if length is less than 5
      // if more than 5, remove the oldest item in the array
    // push the newest product id to the state

  return (
    <div></div>
  );
}

export default RecentlyViewed;